from setuptools import setup, find_packages

setup(
    name='trustflow-sdk',
    version='1.0.0',
    packages=find_packages(),
    python_requires='>=3.7',
    description='TrustFlow SDK for Python',
    # zero dependencies
)
