from setuptools import setup, find_packages

setup(
    name='threatpulse-sdk',
    version='1.0.0',
    packages=find_packages(),
    python_requires='>=3.7',
    description='ThreatPulse SDK for Python',
    # zero dependencies
)
